version: "2"
authtoken: "YOUR_AUTH_TOKEN"  # Replace with your ngrok auth token
tunnels:
  frontend:
    addr: 3000
    proto: http
    inspect: false
  backend:
    addr: 8000
    proto: http
    inspect: false
  dash:
    addr: 8050
    proto: http
    inspect: false 